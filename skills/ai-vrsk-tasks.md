# ai-vrsk-tasks

拉取 VRSK 任务清单全量结构（sections + tasks）。

## 命令

```bash
zero run ai-vrsk-tasks
```

## 产物

- `_brief/vrsk-tasks-YYYY-MM-DD.md`

## 依赖

- PowerShell 7 (`pwsh`)
- `lark-cli` 已登录
- VRSK 任务清单 GUID 已在 `tools/ai-workflow/config.ps1` 配置
